begin_prog
	var(integer a<-1 ,b<-2;
               float c,d;
 			   bit e<-0, f<-1;);

	begin_func int moyenne(integer a, integer b )

		c <- a+b/2;
		return c;
		close_Func moyenne

	begin_func void principal_func()

		d <- moyenne(a,b);
		if d>=2 then
			show(d);
			elsif d<2
			then
			show("hi"d/2,z"hi",a);
		stop_if

		while c<10 then show("test");
		stop_while 

		for ( a<-3; a < 40; a<-a+1)
		 			show("hi"d/2,z"hi",a);
		Stop_for

		switch (a)
		  case 1 : show("chhh");break;
		  case 2 : read(e,c);print_out("chhh");break;
		close_witch

		g<- and(e,f);
		show(g);
		show("fin main function");

	close_Func principal_func


close_prog